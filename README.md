# El Pastor y la Cabra

Este directorio contiene un sitio web estático. Para publicarlo, sube la carpeta completa y conserva `index.html` junto a `assets/`.

El QR debe apuntar a la URL pública del sitio publicado; el archivo ZIP solo sirve para transportar los archivos.
